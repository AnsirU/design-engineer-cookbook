export function componentTsx(name){return `import * as React from 'react';\n\nexport type ${name}Props = React.HTMLAttributes<HTMLDivElement> & { children?: React.ReactNode };\n\n/** Skeleton only. Replace generic markup with the correct accessible primitive before production use. */\nexport function ${name}({children,...props}:${name}Props){return <div data-ds-component=\"${name}\" {...props}>{children}</div>;}\n`;}
export function themeCss(t){return `:root {\n  --ds-bg: ${t.color.bg};\n  --ds-surface: ${t.color.surface};\n  --ds-surface-raised: ${t.color.surfaceRaised};\n  --ds-text: ${t.color.text};\n  --ds-text-muted: ${t.color.textMuted};\n  --ds-border: ${t.color.border};\n  --ds-brand: ${t.color.brand};\n  --ds-focus: ${t.color.focus};\n  --ds-danger: ${t.color.danger};\n  --ds-space-xs: ${t.space.xs};\n  --ds-space-sm: ${t.space.sm};\n  --ds-space-md: ${t.space.md};\n  --ds-space-lg: ${t.space.lg};\n  --ds-space-xl: ${t.space.xl};\n  --ds-space-xxl: ${t.space.xxl};\n  --ds-radius-sm: ${t.radius.sm};\n  --ds-radius-md: ${t.radius.md};\n  --ds-radius-lg: ${t.radius.lg};\n  --ds-control-height: ${t.control.height};\n  --ds-font-family: ${t.typography.fontFamily};\n  --ds-motion-fast: ${t.motion.fast};\n  --ds-motion-base: ${t.motion.base};\n  --ds-motion-easing: ${t.motion.easing};\n}\n`}
export function pageTemplate(name,meta){return `# ${name}\n\n## Purpose\n${meta.description}\n\n## Recommended recipes\n${(meta.recipes||[]).map(x=>`- ${x}`).join('\n')||'- none'}\n\n## Structure\n- persistent context/navigation where needed\n- primary task region\n- secondary context/artifact region when required\n- explicit loading, empty, error, success and interruption states\n\n## Agent contract\nUse this archetype before inventing a new page structure. Read recipe and component docs for exact composition.\n`;}

export function componentTsx(name){return `import * as React from 'react';\n\nexport type ${name}Props = React.HTMLAttributes<HTMLDivElement> & {\n  children?: React.ReactNode;\n};\n\nexport function ${name}({ children, ...props }: ${name}Props) {\n  return <div data-ds-component=\"${name}\" {...props}>{children}</div>;\n}\n`;}
export function themeCss(tokens){return `:root {\n  --ds-bg: ${tokens.color.bg};\n  --ds-surface: ${tokens.color.surface};\n  --ds-text: ${tokens.color.text};\n  --ds-muted: ${tokens.color.muted};\n  --ds-border: ${tokens.color.border};\n  --ds-brand: ${tokens.color.brand};\n  --ds-danger: ${tokens.color.danger};\n  --ds-radius-sm: ${tokens.radius.sm};\n  --ds-radius-md: ${tokens.radius.md};\n  --ds-radius-lg: ${tokens.radius.lg};\n  --ds-space-1: ${tokens.space[1]};\n  --ds-space-2: ${tokens.space[2]};\n  --ds-space-3: ${tokens.space[3]};\n  --ds-space-4: ${tokens.space[4]};\n  --ds-space-5: ${tokens.space[5]};\n  --ds-space-6: ${tokens.space[6]};\n}\n`;}
export function pageTemplate(name){const title=name.split('-').map(x=>x[0].toUpperCase()+x.slice(1)).join(' ');return `# ${title}\n\n## Purpose\nDescribe the user task this template supports.\n\n## Structure\n- App shell / navigation\n- Primary task region\n- Secondary context or utility region\n- Feedback / status\n\n## Required components\nDefine in design-system/templates.json.\n\n## Agent notes\nPrefer this template when the requested task matches its purpose.\n`;}
